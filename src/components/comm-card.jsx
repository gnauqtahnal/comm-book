import React, { Component } from 'react'
import { View, Text } from 'react-native'

export const PlayableCommCard = ({ section, index }) => {}

export const SelectableCommCard = ({ section, index }) => {}
